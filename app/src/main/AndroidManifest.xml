<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	package="com.samoy.treasurechest">

	<uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.INTERNET" />

	<application
		android:allowBackup="true"
		android:fullBackupContent="false"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:networkSecurityConfig="@xml/network_security_config"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.TreasureChest"
		tools:ignore="UnusedAttribute">
		<activity android:name=".activity.VideoCaptureActivity" />
		<activity android:name=".activity.VideoCompressActivity" />
		<activity android:name=".activity.VideoDownloadActivity" />
		<activity android:name=".activity.VideoSwitchActivity" />
		<activity android:name=".activity.VideoComposeActivity" />
		<activity android:name=".activity.VideoExtractActivity" />
		<activity android:name=".activity.MainActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
	</application>

</manifest>
